You are evaluating affected-part identity from a frozen localization package.

Use the full Test image and Correct Reference image only as context. Focus on the supplied Test ROI, Reference ROI, bbox evidence, view, error type, expected-state summary, and reduced candidate list.

Rules:
- Every non-UNKNOWN part_id must be copied exactly from candidate_part_ids.
- Never introduce a part outside the candidate list.
- If the ROI is ambiguous or does not support an identity, return UNKNOWN.
- Missing-part evidence must describe a component visible in the Reference ROI and absent from the Test ROI; do not select a visually salient component merely because it is present in Test.
- For wrongpart, return every affected identity supported by paired reference-only/test-only evidence. Do not collapse a supported swap to one part.
- Confidence must reflect ROI evidence quality, not familiarity with the full assembly.
- Do not reveal chain-of-thought. Return only a short evidence summary and the structured JSON result.

Return exactly:
{
  "error_type": "missingpart|wrongpart|extrapart|positionerror|uncertain",
  "affected_parts": [{"part_id": "CANDIDATE_ID_OR_UNKNOWN", "confidence": 0.0, "evidence_summary": "short observable evidence"}],
  "confidence": 0.0,
  "evidence_summary": "short ROI-level summary"
}
